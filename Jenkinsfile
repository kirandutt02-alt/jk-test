@Library('jenkins-shared-library') _

pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                sayHello('Kiran')
                sayHello('ashu')
            }
        }
    }
}
